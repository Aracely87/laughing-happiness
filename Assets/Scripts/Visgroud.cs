using System.Collections;
using System.Collections.Generic;
using UnityEngine;

/* The above class is a C# script for a Unity game object that includes empty Start and Update methods. */
public class Visgroud : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
